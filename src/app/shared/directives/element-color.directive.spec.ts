import { ElementColorDirective } from './element-color.directive';

describe('ElementColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementColorDirective();
    expect(directive).toBeTruthy();
  });
});
